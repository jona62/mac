class Circle {
    init(r) {
        this.radius = r;
    }
    area() {
        return 3.14159 * this.radius * this.radius;
    }
}
val c = Circle(5);
print c.radius;           // expect: 5
print c.area();           // expect: 78.5397
