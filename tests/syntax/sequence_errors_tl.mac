// Timeline inside grid should error
var tl = timeline loop {
    @blank "A" : 500ms
    --- crossfade 150ms ---
    @blank "B" : 500ms
};

grid 2x1 { tl @blank "ok" } => "fail.png";
// expect runtime error: Cannot use Timeline as a frame
