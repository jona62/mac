// Duration
//
// Time in milliseconds for animation frames.
// Supports +, *, == operators.

class Duration {
    init(ms);

    // Duration in milliseconds.
    ms;

    __add__(other) -> Duration;
    __mul__(n) -> Duration;
    __eq__(other) -> bool;
}
